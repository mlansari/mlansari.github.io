"""Setup for the site"""
from setuptools import setup

setup(
    name='Mohamed Lansari Site',
    packages=['mlansari'],
    include_package_data=True,
    install_requires=[
        'flask'
    ],
)